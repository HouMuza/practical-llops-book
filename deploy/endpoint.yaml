$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineEndpoint.schema.json
name: qwen3-yourname-prod
auth_mode: key
# Traffic is set by `make deploy-blue` (--all-traffic) or traffic-split targets.
